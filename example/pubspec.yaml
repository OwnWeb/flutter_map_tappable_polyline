name: example
description: A new Flutter project.
version: 5.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_map_tappable_polyline:
    path: ../

flutter:
  uses-material-design: true
